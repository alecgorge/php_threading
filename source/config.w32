// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("threading", "for threading support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("threading", "enable threading support", "no");

if (PHP_THREADING != "no") {
	EXTENSION("threading", "threading.c");
}

